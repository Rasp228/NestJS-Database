<div class="container">
    <table class="table">
        <tr>
            <th>ID_materialy_rosliny</th>
            <th>ID_Materialy</th>
            <th>ID_Rosliny</th>
            
        </tr>
            {{#each materialy_rosliny}}
                <tr>  
                    <td>{{this.ID_materialy_rosliny}}</td>
                    <td>{{this.ID_Materialy}}</td>
                    <td>{{this.ID_Rosliny}}</td>
                    {{ user.isAdmin }}
                    {{#if ../user.isAdmin}}
                    <td><a href="edit/{{this.ID}}">Edytuj</a> <a href="delete/{{this.ID}}">Usuń</a></td>
                    {{/if}}
            {{/each}} 
    </table>
    <a href="add/">Dodaj</a>
</div>